/**
 * Esta lista trabalha no método para adicionar equipamento. A lista invocada com esta interface serve para representação
 * no ecrã da lista de equipamento para ser exibida na tela para o usuário.
 */
export interface ListaEquipamentoApresentacaoInterface {
  id: number,
  descricao: string,
  quantidade: number
}
