/**
 * Esta lista trabalha no método para adicionar equipamento. A lista criada lanças os dados que serão posteriormente
 * lançados no JSON para serem enviados na requisição
 */
export interface ListaEquipamentoInterface {
  // id: number,
  descricao: string,
  quantidade: number
}
